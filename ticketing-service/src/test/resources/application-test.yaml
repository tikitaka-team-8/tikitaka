spring:
  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true
    validate-on-migrate: true
    out-of-order: false
    clean-disabled: true

clients:
  platform-service:
    service-key: platform-internal-service-key-for-tests
