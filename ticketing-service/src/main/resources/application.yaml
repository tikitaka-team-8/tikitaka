server:
  port: 8082

spring:
  application:
    name: ticketing-service

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
  endpoint:
    health:
      probes:
        enabled: true
