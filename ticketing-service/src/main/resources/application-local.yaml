spring:
  config:
    activate:
      on-profile: local
  datasource:
    url: jdbc:postgresql://localhost:5434/tikitaka_ticketing
    username: ticketing
    password: ticketing_local_password
  data:
    redis:
      host: localhost
      port: 6381
  kafka:
    bootstrap-servers: localhost:9092
