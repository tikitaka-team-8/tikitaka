spring:
  config:
    activate:
      on-profile: staging
  datasource:
    url: jdbc:postgresql://${STAGING_DATA_PRIVATE_IP}:5435/${PAYMENT_DB_NAME}
    username: ${PAYMENT_DB_USERNAME}
    password: ${PAYMENT_DB_PASSWORD}
  kafka:
    bootstrap-servers: ${STAGING_DATA_PRIVATE_IP}:9092

clients:
  ticketing-service:
    url: ${TICKETING_SERVICE_URL}

# 일반 API와 분리된 모니터링용 Actuator 관리 포트
# health 및 Prometheus 메트릭 엔드포인트만 노출
management:
  server:
    port: 9000
  endpoints:
    web:
      exposure:
        include: health,info,prometheus
