spring:
  config:
    activate:
      on-profile: docker
  datasource:
    url: jdbc:postgresql://payment-postgres:5432/tikitaka_payment
    username: payment
    password: payment_local_password
  kafka:
    bootstrap-servers: kafka:19092
