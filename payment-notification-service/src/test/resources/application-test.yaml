spring:
  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true
    validate-on-migrate: true
    out-of-order: false
    clean-disabled: true

clients:
  ticketing-service:
    url: http://localhost:8082
