spring:
  jpa:
    hibernate:
      ddl-auto: validate

  flyway:
    enabled: true
    validate-on-migrate: true
    out-of-order: false
    clean-disabled: true

auth:
  token:
    secret: MDAwMDAwMDAwMDAwMDAwMDAwMDAwMDAwMDAwMDAwMDA=
    access-token-expiration: 15m
    refresh-token-expiration: 14d

internal:
  service:
    key: platform-internal-service-key-for-tests
