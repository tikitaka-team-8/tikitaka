spring:
  config:
    activate:
      on-profile: docker
  datasource:
    url: jdbc:postgresql://platform-postgres:5432/tikitaka_platform
    username: platform
    password: platform_local_password
  data:
    redis:
      host: platform-redis
      port: 6379
  kafka:
    bootstrap-servers: kafka:19092
