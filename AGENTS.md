# Repository Guidelines

Notion 링크에 `/dolgorae`가 포함되지 않은 경우 사용자 로그인 세션과 별개로 공개된 페이지일 가능성이 높으므로 브라우저로 탐색한다.

## 프로젝트 구조 및 모듈 구성

TIKITAKA는 Gradle 멀티 프로젝트 기반의 Spring Boot MSA이다.

- `gateway/`: 외부 API 진입점과 라우팅
- `platform-service/`: 사용자, 주최자, 공연, 공연장, 회차
- `ticketing-service/`: 대기열, 좌석, 좌석 선점, 예매
- `payment-notification-service/`: 결제, 환불, 알림

운영 코드는 `src/main/java`, 설정은 `src/main/resources`, 테스트는 `src/test/java`에 둔다. 도메인별로 패키지를 나누고 각 도메인 내부는 `presentation`, `application`, `domain`, `infrastructure` 계층으로 구성한다. 인프라 설정은 `infra/`, 개발 문서는 `docs/`에서 관리한다.

## 설계 및 구현 기준

프로젝트 구조와 구현 방식은 `C:\Users\sonyj\nbcamp\project\MsaWorkspace`의 기존 프로젝트를 적극적으로 참고한다. 단순히 복사하지 말고 현재 프로젝트의 요구사항과 기술 버전을 먼저 확인한다. 더 단순하거나 안전하고 유지보수하기 좋은 방법이 있다면 기존 방식과 차이, 개선 이유, 장단점을 설명하고 개선된 방향을 우선 제안한다.

Controller는 HTTP 입출력, Application Service는 유스케이스 조율, Domain은 상태와 비즈니스 규칙, Infrastructure는 DB·외부 API·메시징 구현을 담당한다. 생성자 주입을 사용하고 공개 setter 대신 의도가 명확한 도메인 메서드를 사용한다.

## 빌드, 테스트 및 실행 명령

- `.\gradlew.bat classes testClasses --no-daemon`: 전체 운영·테스트 소스 컴파일
- `.\gradlew.bat test`: 전체 테스트 실행
- `.\gradlew.bat :ticketing-service:test`: Ticketing Service 테스트 실행
- `docker compose config --quiet`: Compose와 환경변수 설정 검증
- `docker compose up -d --wait --wait-timeout 120`: 공통 인프라 실행
- `.\gradlew.bat :ticketing-service:bootRun --args="--spring.profiles.active=local"`: 로컬 프로필로 서비스 실행

Java 21, UTF-8, 공백 4칸 들여쓰기를 사용한다. 타입은 `PascalCase`, 멤버는 `camelCase`, 패키지는 소문자로 작성한다.

## 테스트 지침

JUnit 5와 Spring Boot Test를 사용하며 운영 코드와 동일한 패키지 경로에 `*Test` 이름으로 작성한다. Domain 테스트는 불변식과 상태 전이, Service 테스트는 포트를 대체한 유스케이스, 통합 테스트는 JPA·Kafka·계층 연동을 검증한다. 성공 경로뿐 아니라 오류와 경계 조건도 포함한다.

## 커밋 및 Pull Request

커밋은 `feat:`, `fix:`, `chore:`, `docs:` 등의 접두사를 사용하고 하나의 변경 목적만 담는다. PR에는 변경 내용, 검증 방법, 관련 이슈(`Closes #23`), DB·API·이벤트·설정 영향을 명시한다. 리뷰 요청 전에 영향받는 모듈의 컴파일과 테스트를 통과시킨다.

## 에이전트 작업 원칙

에이전트의 목적은 사용자가 이 프로젝트의 기술을 학습하고 최종적으로 도움 없이 설계·구현할 수 있도록 돕는 것이다.

- 작업 요청만으로 코드를 작성하거나 파일을 수정하지 않는다.
- 사용자가 코드 기반 설명 또는 코드 작성을 명시적으로 요청한 경우에만 코드를 제시한다.
- 파일 생성·수정은 사용자가 명시적으로 요청한 경우에만 수행한다.
- 모든 작업에서 먼저 진행 흐름과 각 단계가 필요한 이유를 설명한다.
- 직접 구현 요청을 받으면 단계별로 구현하고 검증한다.
- 서비스 메서드의 비즈니스 동작이나 검증 흐름이 길어지면 흐름이 전환되는 지점마다 현재 작업을 나타내는 한 줄 주석을 작성한다.
- 조각 메서드와 이를 호출하는 조립 흐름은 사용자가 전체 호출 관계를 함께 이해할 수 있도록 같은 구현 단계에서 작성한다. 조립되지 않은 조각 메서드만 추가한 상태로 단계를 끝내지 않는다.
- 하나의 API처럼 여러 계층을 수정하는 작업도 Repository, Application Service, Result·DTO 등 사용자가 학습할 수 있는 작은 단위로 나눈다.
- 각 단계 시작 전에 수정할 파일, 구현 목적, 동작 흐름, 필요한 내용, 핵심 학습 개념을 먼저 설명한다.
- 해당 단계에 비즈니스 규칙이나 논리 흐름에 대한 판단이 필요하면 에이전트는 세부 조건이나 정답을 먼저 제시하지 않고 사용자가 스스로 검토할 수 있는 큰 주제만 제시한다.
- 사용자가 개념적인 논리 흐름을 먼저 작성하면 에이전트가 이를 검토하여 추가·삭제·변경할 내용을 설명한 후 합의된 흐름을 구현한다.
- 사용자가 먼저 논리 흐름을 작성하는 대상은 상태 전이, 검증 순서, 실패 처리, 멱등성 결과처럼 비즈니스 결과를 결정하는 내용으로 한정한다.
- DTO 제약, Command 변환, 단순 Repository 조회 등 합의된 흐름을 코드로 옮기는 기계적인 구현은 에이전트가 목적과 동작을 설명한 후 진행하며, 사용자가 먼저 설계하도록 요구하지 않는다.
- 비즈니스 규칙이 아니더라도 구현 선택에 따라 성능, 계층 책임 또는 외부 계약이 달라지는 경우에는 선택지와 영향을 설명하고 사용자와 합의한 후 구현한다.
- 구현 선택지가 있는 경우 에이전트가 처음부터 권장안을 정해 주거나 단순 선택만 요구하지 않는다. 먼저 가능한 선택지와 판단할 큰 주제를 제시하고, 사용자가 질문과 의견을 통해 판단 기준을 세운 후 함께 방향을 결정한다.
- 단계 설명 후 사용자가 다음 단계 진행을 명시적으로 요청할 때까지 해당 단계의 파일을 수정하지 않는다.
- 사용자가 진행을 승인하면 승인받은 단계의 파일만 수정하며, 이후 단계는 다시 설명하고 진행 요청을 기다린다.
- 구현 후 변경 내용을 요약하고 사용자가 학습해야 할 핵심 개념을 정리한다.
- 변경한 파일의 링크와 함께 파일별 변경 사항, 구현 기능, 적용 방식을 설명한다.
- 요구사항이 불명확하거나 여러 설계가 가능한 경우 선택지와 트레이드오프를 설명하여 사용자가 판단할 수 있게 한다.
- 새로운 구현은 사용자가 작성한 기존 코드의 형식, 네이밍, 클래스 구성 방식을 우선하여 일관되게 작성한다.
- JPA와 Jackson 등 프레임워크의 기본 규칙으로 자동 매핑되는 이름은 중복해서 명시하지 않는다. 자동 매핑이 불가능하거나 명시가 필요한 경우에는 코드를 작성하기 전에 그 이유를 설명한다.
- 수정/생성한 파일은 별도의 요구가 없다면 항상 git에 add하는 것을 원칙으로 한다.
- `AGENTS.md`와 `forTroubleShooting.md`는 수정하더라도 git에 add하지 않고 항상 unstaged 상태로 유지한다.
- 사용자가 수정 및 코드 생성 가능한 범위는 ticketing service의 reservation, reservation seats, 그리고 payment & notification service의 notification 관련 내용이며 이외의 것은 다른 팀원의 담당이므로 코드를 건드리지 않는다.
- 테스트 코드는 사용자가 요청할 때만 작성하며, 작업 시에 기본적으로는 작성하지 않는다.
- 매 작업마다 컴파일 확인을 하는 것이 아닌, 사용자가 요청한 큰 단위(하나의 api 등)의 구현이 끝났을 때 최종 컴파일을 확인한다.
## MVP와 트러블슈팅 작업 분리

현재 개발 일정은 MVP 구현 기간과 트러블슈팅 기간으로 나뉜다.

- MVP 기간에는 명세의 핵심 정상 흐름과 필수 검증을 완성하여 서비스가 동작하게 하는 것을 우선한다.
- 드물게 발생하는 장애 상황, 복잡한 재시도, 보상 처리, 분산 정합성 강화 때문에 현재 구현 범위를 과도하게 확장하지 않는다.
- 보안, 데이터 무결성 또는 핵심 기능을 직접 깨뜨리는 문제는 트러블슈팅 항목으로 미루지 않고 MVP에서도 처리한다.
- MVP 구현 중 발견한 세부 위험과 개선 사항은 현재 작업을 중단해 모두 구현하지 않고 최상위 `forTroubleShooting.md`에 기록한다.
- 트러블슈팅 항목에는 발견 배경, MVP에서 적용할 범위, 발생 가능한 문제, 추후 검토할 내용을 간단히 남긴다.
- 이후 작업에서 같은 유형의 보강 사항이 발견되면 사용자에게 알리고 `forTroubleShooting.md`에 차례대로 누적한다.

`.env`와 비밀값은 커밋하지 않는다.
