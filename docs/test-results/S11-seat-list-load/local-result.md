## 성능·부하 실행 결과

### 부하 조건
- 대상 시나리오 / API: S11-B. 대량 데이터에서 인기 회차 좌석을 조회하는 사용자들 — `GET /api/v1/schedules/{eventSessionId}/seats` (ticketing-service, 직접 호출 · Gateway 미경유)
- 부하 모델: 계단식 부하(Step Load) — 50 → 100 → 300 → 500 → 1,000 VU를 단계별로 증가시키며 각 단계를 독립 실행
- 테스트 도구 및 버전: k6 (버전은 실행 환경에서 `k6 version`으로 직접 확인 필요 — 이 문서 작성 환경에는 k6 미설치라 확인 불가)
- 실행 시간: 2026-09-15 19:26 ~ 20:04경 (결과 파일 생성 시각 기준 추정, 약 38분. 워밍업 단계 포함하면 이보다 조금 더 김)
- VU 또는 도착률: 50 / 100 / 300 / 500 / 1,000 (5단계, 단계별 독립 실행)
- Ramp-up: 단계별 30s (LOAD_RAMP)
- 지속 시간: 단계별 유지 2m + ramp-down 30s (총 3m/단계)
- Think Time: 1s (POLL_INTERVAL — VU가 좌석 목록을 반복 조회하는 주기)
- Polling 주기: Think Time과 동일 (1s)
- 총 요청 수: 139,228건 (5단계 합산: 7,435 / 14,788 / 39,990 / 41,750 / 35,265 — 1,000 VU 단계는 대량 타임아웃으로 반복 수가 줄어든 상태로 조기 종료됨)
- 테스트 데이터 규모: 응답 1건 평균 약 424~460KB (아래 표 참고) — 정확한 시드 좌석 수는 `SELECT count(*) FROM p_schedule_seat WHERE event_session_id='31000000-0000-0000-0000-000000000001'` 로 재확인 필요 (대량 데이터 조회 조건은 응답 크기로 볼 때 충족된 것으로 보이나 정확한 건수 미기록)

### 성능 결과
동일 조건(단일 API, 필터 없음)에서 VU 단계별로 측정한 결과입니다.

| VU | 평균 | p50 | p95 | p99 | 최대 | 오류율 | 전체 RPS | 업무처리 TPS | Timeout 수 |
|---|---|---|---|---|---|---|---|---|---|
| 50 | 31ms | 19ms | 87ms | 149ms | 258ms | 0.01% | 40.8 | 40.0 | 0 |
| 100 | 40ms | 14ms | 156ms | 248ms | 1,074ms | 0.04% | 80.9 | 78.9 | 0 |
| 300 | 169ms | 97ms | **507ms** | 622ms | 888ms | 0.42% | 213.2 | 203.5 | 0 |
| 500 | 884ms | 771ms | **2,220ms** | 3,249ms | 5,002ms | **1.66%** | 217.5 | 196.7 | 1 |
| 1000 | 2,467ms | 2,235ms | **5,001ms** | 5,004ms | 5,093ms | **30.46%** | 236.1 | 95.9 | 10,740 |

- SLA 기준(p95<500ms, 오류율<1%) 대비: 50/100 VU는 여유 있게 통과, 300 VU는 p95가 6.7ms 차이로 **최초 위반**(오류율은 통과), 500 VU부터 p95·오류율 **모두 위반**, 1,000 VU는 p95가 k6 클라이언트 타임아웃 상한(5초)에 그대로 걸리고 요청의 30%가 응답 자체를 못 받는 수준(시스템 사실상 포화).
- 1,000 VU 구간의 Timeout 수(10,740)는 오류율(30.46%)과 거의 일치 — 대부분의 실패가 "잘못된 응답"이 아니라 "응답 자체를 못 받은" 순수 타임아웃임.
- RPS는 500 VU(217.5)와 1,000 VU(236.1)가 거의 같아, 실질 처리량이 220 안팎에서 이미 포화되고 그 이상 VU를 늘려도 대기만 늘어나는 패턴으로 보임.

### 서버·애플리케이션
- Gateway CPU / Memory: 확인 필요 (이번 테스트는 Gateway 미경유 — 직접 ticketing-service 호출이라 해당 없음)
- 서비스별 CPU / Memory: 확인 필요 — Grafana `TIKITAKA Load Test - Service` 대시보드에서 이번 테스트 시간대(19:26~20:04)로 범위를 맞춰 직접 조회 필요
- JVM Heap: 확인 필요 (위와 동일하게 Grafana에서 조회 필요)
- GC: 확인 필요 (별도 GC 메트릭 미설정 — `management.metrics.distribution` 관련 설정이 프로젝트에 없어 percentile/GC 상세 지표 자체가 Prometheus에 없음)
- 재시작 / OOM: 확인 필요 (`docker ps` / `docker logs`로 재시작 이력 확인 권장)

### PostgreSQL
- Connection Pool: 프로젝트 전체에 HikariCP `maximum-pool-size` 명시적 설정이 없음 → Spring Boot 기본값(10)으로 추정됨(실측 확인 필요). 500~1,000 VU 구간의 응답시간 급증·타임아웃 패턴이 커넥션 풀 고갈 시 나타나는 전형적 신호와 일치 — **유력 원인 후보**이나 아직 튜닝 재검증 실험은 미실시
- Lock Wait: 확인 필요 (측정 안 함)
- Slow Query: 확인 필요 (`EXPLAIN ANALYZE` 등 쿼리 실측 안 함)
- 트랜잭션 실패: 확인 필요

### Redis
- 응답 지연: 확인 필요 (큐 토큰 조회에 Redis 사용 — 부하 중 응답 지연 별도 측정 안 함)
- Memory: 확인 필요
- 명령 처리량: 확인 필요

### Kafka
- 좌석 조회(`GET .../seats`) 요청 경로는 코드상 Kafka를 전혀 사용하지 않음(SeatService가 큐 토큰 검증 + DB 조회만 수행) → 이번 테스트 범위에서는 **해당 없음**

### 병목 분석 (2026-09-15 기준 — 9/16 재검증 결과로 원인 정정됨, 하단 업데이트 섹션 참고)
- 최초 병목 지점: 300 VU에서 p95 SLA 최초 위반, 500 VU에서 p95·오류율 동시 위반 — 실질적 한계는 **300~500 VU 사이**
- 판단 근거: 위 표의 p95/오류율 추이(300 VU 507ms→500 VU 2,220ms로 4.4배 급증, 오류율 0.42%→1.66%), RPS가 500 VU 이후 정체(217→236)
- 추정 원인: HikariCP 커넥션 풀 미설정(기본값 10개 추정)으로 인한 DB 커넥션 대기 큐잉 — **가설 단계, 풀 크기 상향 후 재측정으로 검증 필요** (아직 미실시)
- 안정적으로 처리 가능한 범위: 현재 설정 기준 100~300 VU (300 VU도 p95는 SLA 경계선에 걸쳐 있어 보수적으로는 100~200 VU를 안전 범위로 권장)
- 부하 종료 후 정상화 여부: 확인 필요 (`pg_stat_activity` 활성 커넥션 수 등 회복 여부 기록 안 남음 — wrapper 스크립트에 체크 로직은 있으나 값 미기록)

### 판정
- 테스트 결과: **FAIL** (300 VU부터 p95 SLA 위반 시작, 500 VU부터 오류율까지 동시 위반)
- 진행 상태: **분석 중** (병목 원인이 가설 단계 — HikariCP 등 서버 리소스 실측 및 튜닝 재검증 필요)
- 판정 근거: p95<500ms·오류율<1% 기준을 300 VU부터 벗어나기 시작해 500 VU에서 명확히 위반, 1,000 VU는 사실상 서비스 불능(오류율 30%) 수준
- 추가 확인 사항:
  - Grafana에서 재검증 시간대의 CPU/JVM Heap 추이 확인 (리소스 포화 여부로 원인 교차검증)
  - PostgreSQL Lock Wait / Slow Query 실측
  - 부하 종료 후 커넥션/스레드 정상화 여부 기록
  - Redis 응답 지연 측정 (큐 토큰 조회 경로)
  - ~~정확한 시드 좌석 수 재확인~~ → 3,008석으로 확인됨
  - ~~PostgreSQL 실제 HikariCP 풀 크기 확인 및 상향 후 500/1,000 VU 재측정~~ → 재측정 결과 HikariCP는 원인이 아니었음 (아래 업데이트 섹션 참고). 풀 크기는 참고용으로 20까지만 올려둔 상태

---

## 🔄 업데이트 (2026-09-16): 페이지네이션 적용 후 재측정

### 원인 재진단: 응답 크기(행 수)가 핵심이었음
9/15 결과 이후 재검증에서 다음을 확인함:
- `SeatController`/`SeatService`/`ScheduleSeatJpaRepository.findSeats`가 페이지네이션 없이 **세션의 전체 좌석(3,008석)을 매 요청마다 정렬·직렬화**해서 내려주고 있었음 (응답 1건 평균 ~450KB)
- k6의 `http_req_waiting`(TTFB)과 `http_req_receiving`(바디 전송 시간)이 세션 간 비례해서 함께 나빠짐 → 좁은 DB 커넥션 풀 문제보다는 응답 크기(직렬화·전송량)에 비례한 범용 부하로 판단됨
- HikariCP `maximum-pool-size` 미설정(추정 기본값 10) 가설은 유력 후보였지만, 최종적으로는 **응답 행 수 축소가 훨씬 결정적**이었음 (풀 크기는 참고용으로 20까지만 올려둠, `application-docker.yaml`)

### 조치: 좌석 목록에 페이지네이션 도입
- `GET /api/v1/schedules/{eventSessionId}/seats`에 `page`(기본 0)/`size`(기본 50, 최대 200) 쿼리 파라미터 추가
- `ScheduleSeatJpaRepository.findSeats`를 `Page<ScheduleSeat>` 반환으로 변경 (LIMIT/OFFSET + 별도 countQuery), 응답은 `ReservationController`와 동일한 패턴으로 `ApiResponse<List<ScheduleSeatResponse>> + PageMeta`
- k6 스크립트(`seat-list-load.js`)에 `PAGE_SIZE`/`PAGE_NUMBER` 환경변수 추가, 요청에 자동으로 `page=&size=` 부여

### 재측정 결과 (페이지네이션 적용 후, size=50 기본값)

| VU | p95 | 오류율 | 응답 크기(평균) | 비고 |
|---|---|---|---|---|
| 300 | 22.48ms | 0.26% | ~7.9KB | 이전(507ms) 대비 대폭 개선 |
| 500 | 53.66ms | 0.78% | ~7.9KB | 이전엔 미완주였음 |
| 1000 | 214.06ms | **0.00%** | ~7.98KB | 아래 테스트 이슈 수정 반영 후 수치 |

이전(9/15) 대비 응답 크기는 **~450KB → ~7.9KB로 약 98% 감소**했고, p95는 전 구간에서 10~수십 배 개선됨.

### 별도로 발견한 테스트 이슈: 큐 admission 토큰 TTL vs 부하 유지시간 충돌 (서버 성능과 무관)
페이지네이션 적용 직후에도 1000 VU에서 `http_req_failed`가 2.67~3.32%로 임계치(1%)를 넘는 현상이 남아있었음. 로그 확인 결과 지연이 아니라 명시적 비즈니스 예외였음:
```
비즈니스 예외 발생: code=Q-001, message=대기열 입장 권한이 없습니다.
```
원인은 `QueueService.validateAndEnterInternal`이 이미 입장(ENTERED)한 사용자라도 **최초 admission 토큰이 `admission-token-ttl`(기본 PT3M=3분) 만료되면** 이후 요청을 전부 Q-001로 거부하는 구조 때문. 기존 부하 스크립트의 1000 VU 단계는 mint(토큰 발급) 20~21초 + ramp 30s + 유지 2m + ramp-down 30s로 3분을 넘겨 TTL을 그대로 침범했음 (예외 발생 시각을 역산하면 mint 시작 후 196~203초 지점 — TTL 경계와 일치).

**이건 서버 용량 문제가 아니라 테스트 설계 결함으로 판단**하여 서버 코드는 건드리지 않고 테스트 스크립트만 수정함:
- `scripts/test-scenarios/s11-seat-list-load/run-seat-list-load-steps.sh`, `scripts/test-scenarios/s11-seat-list-load/seat-list-load.js`의 `LOAD_RAMP`/`LOAD_DURATION` 기본값을 `30s`/`2m` → `20s`/`1m`으로 단축 (단계 합계 3분 → 1분40초, TTL까지 약 50~55초 여유 확보)
- 재실행 결과: 1000 VU에서 `http_req_failed` **0.00%**(0/86,547), `checks_succeeded` 100%로 완전 통과

⚠️ 다만 유지 구간이 2분→1분으로 함께 줄어들어서, 직전 1000 VU 결과(p95 409ms)와 이번 결과(p95 214ms)를 곧바로 "서버가 2배 빨라졌다"고 비교하긴 어려움(완전히 동일한 조건은 아님). p95 개선의 실질적 근거는 페이지네이션(응답 크기 축소) 쪽이고, 이번 1000 VU 결과는 "페이지네이션 적용 후 1000 VU까지 SLA를 만족한다"는 확인 용도로 해석하는 게 정확함. 완전히 깨끗한 before/after가 필요하면 유지 구간을 동일하게 맞추고 TTL만 테스트 환경 한정으로 늘려서 재검증하는 걸 추천.

### 남은 후속 작업
- [ ] 이미 ENTERED된 사용자가 admission 토큰 만료와 무관하게 좌석 조회를 계속할 수 있어야 하는지 팀 논의 (현재는 3분 지나면 Q-001로 거부됨 — 실제 서비스에서도 발생 가능한 케이스)
- [ ] Grafana CPU/JVM Heap, PostgreSQL Lock Wait/Slow Query, Redis 응답 지연 — 여전히 미측정
- [ ] 유지 구간을 동일하게 맞춘 깨끗한 before/after 재검증 (선택)

### 최신 판정 (2026-09-16 기준)
- 테스트 결과: **PASS** (300/500/1,000 VU 모두 p95<500ms, 오류율<1% 충족)
- 이 판정은 위 페이지네이션 조치 + 테스트 스크립트 수정을 모두 반영한 결과임

---

## 🔄 업데이트 (2026-09-16~17): 이후 재실행에서 재확인된 이슈

### 2026-09-16 11:04 실행(`run_110402.log`, artifacts/k6/s11-seat-list-load/results/20260916_110402/): 페이지네이션 미적용 재발 (일시적으로 추정)
- 이 실행은 `LOAD_RAMP=30s`/`LOAD_DURATION=2m`(구 기본값, 단계 합계 3분)로 진행됨 — 현재 스크립트 기본값(20s/1m)과 다름.
- 50/100/300 VU 응답 크기가 평균 **~468KB**로, 페이지네이션 적용 전(9/15) 수준으로 되돌아가 있었음 (`seat_response_size` avg 468,199B @50VU, 468,090B @100VU, 465,639B @300VU).
- 그 결과 300 VU에서 이미 p95 1.59s(기존 threshold 크게 위반), 500 VU 진입 직후부터 "request timeout"(5s 타임아웃) 경고가 대량 발생 — 로그는 500 VU 단계 진행 중(2m35s/3m)에 끊겨 있고 `500vu.json`/`1000vu.json`은 생성되지 않음(비정상 종료 또는 중도 중단으로 추정). Q-001(토큰 만료) 예외는 이번엔 **0건**으로, TTL 문제가 아니라 순수 응답 크기/속도 문제였음.
- **원인 추정**: ticketing-service 컨테이너가 페이지네이션 반영 전 이미지로 재기동됐거나(재빌드 누락), 혹은 다른 원인으로 `page`/`size` 파라미터가 서버에서 무시된 것으로 보임. Dockerfile이 빌드 시점 `src`를 이미지에 굽는 구조라, 코드만 고치고 컨테이너를 재빌드하지 않으면 이런 현상이 재현될 수 있음.

### 2026-09-17 03:26 실행(`run_032606`, artifacts/k6/s11-seat-list-load/results/20260917_032606/): 페이지네이션은 정상 적용, 그러나 새로운 p95 한계 확인
- `LOAD_RAMP=20s`/`LOAD_DURATION=1m`(현재 기본값)로 정상 진행, 전 구간 응답 크기 **~7,979B로 일관** — 페이지네이션 자체는 정상 동작.
- 오류율은 전 구간 0%에 가까움(500 VU에서만 0.006%) — Q-001도, 타임아웃도 없음.
- 그런데 **p95가 300 VU부터 다시 SLA(500ms)를 위반**: 300 VU p95=687ms, 500 VU p95=1.57s, 1,000 VU p95=2.28s (avg도 1.15s까지 증가). 9/16 업데이트에서 "PASS"로 판정했던 1,000 VU 결과(p95 214ms, 0% 오류)와 응답 크기는 동일한데 지연시간만 크게 다름.
- **해석**: 응답 크기(직렬화·전송량) 문제는 해결됐지만, 별개로 **동시 요청 수 자체가 늘어나면 지연시간이 선형 이상으로 증가하는 한계**가 존재하는 것으로 보임 — DB 커넥션 풀 대기, CPU 포화, 혹은 단일 인스턴스의 처리량 한계 등 여러 가설이 가능하나 아직 Grafana/HikariCP 지표로 교차검증하지 않음. 9/16 "PASS" 판정 당시와 조건(서버 상태, 동시 부하, 테스트 데이터 규모)이 완전히 같았는지도 불확실함.

### 최신 판정 재조정 (2026-09-17 기준)
- 테스트 결과: **재확인 필요 (기존 PASS 판정 보류)** — 페이지네이션으로 응답 크기 문제는 해결됐으나, 이후 재실행에서 300 VU 이상 구간의 p95 SLA 위반이 다시 관측됨.
- 후속 작업:
  - [ ] ticketing-service가 최신(페이지네이션 반영) 이미지로 떠 있는지 배포 확인 절차를 결과 문서에 남기기 (Commit SHA/Docker Image Tag 기록 필요 — 현재 미기록)
  - [ ] 300~1,000 VU 구간 p95 급증의 원인을 Grafana(HikariCP/CPU/JVM Heap)로 교차검증
  - [ ] 9/16 "PASS" 실행과 9/17 실행 사이 테스트 데이터 규모(좌석 수)·서버 상태가 동일했는지 확인 후, 필요하면 동일 조건으로 재측정

---

## 🔄 업데이트 (2026-09-17): 필드 프로젝션 + 짧은 TTL 캐시 적용 후 1,000 VU 재측정

### 테스트 조건
- 코드 상태: `SeatListReader.PROJECTION_ENABLED = true`(필요한 7개 컬럼만 JPQL 프로젝션 조회), `SeatListCacheConfig.CACHE_ENABLED = true` + `CACHE_TTL_SECONDS = 2`(Caffeine, in-memory) — 둘 다 켠 상태에서 측정.
- 이번 실행은 5단계 wrapper(`run-seat-list-load-steps.sh`)를 쓰지 않고, **1,000 VU 단계만 k6로 직접 재실행**함 (`--env SCENARIO=load --env LOAD_VUS=1000 --env LOAD_RAMP=20s --env LOAD_DURATION=1m`). 그래서 300/500 VU에 대한 이번 조합의 새 데이터는 없음 — 아래는 1,000 VU 단일 지점 비교임.
- 요청: `GET /api/v1/schedules/{eventSessionId}/seats?page=0&size=50` (필터 미적용), 실행 시각 2026-09-17 03:28:28~03:30:30 (KST), 총 296,822 요청(순수 좌석 조회 반복 286,322건 — 나머지는 setup() 큐 토큰 발급/폴링).
- 원본 로그: `artifacts/k6/s11-seat-list-load/logs/manual_1000vu_032828.log`, 요약 JSON: `artifacts/k6/s11-seat-list-load/manual/1000vu_032828.json`.

### 결과: 9/17(페이지네이션만 적용) 1,000 VU 대비 비교

| 구분 | avg | p50 | p95 | p99 | max | 오류율 | RPS | 업무 TPS(iterations/s) |
|---|---|---|---|---|---|---|---|---|
| 9/17 (페이지네이션만) | 1,145.8ms | 1,266.5ms | 2,284.9ms | 2,763.4ms | 4,552.6ms | 0.00% | 578.4 | 494.1 |
| 9/18 (+ 프로젝션 + 캐시) | 269.8ms | 146.1ms | 1,021.6ms | 1,812.0ms | 4,911.7ms | 0.00% | 2,446.0 | 2,359.5 |
| 변화 | **약 4.25배 감소** | 약 8.66배 감소 | **약 2.24배 감소** | 약 1.52배 감소 | 거의 동일(오히려 소폭 증가) | 동일(0%) | **약 4.23배 증가** | 약 4.77배 증가 |

- **오류율은 두 실행 모두 0%** — 기능적으로는 문제없이 전부 성공 처리됨.
- RPS/TPS가 큰 폭으로 늘어난 이유는 "더 많은 부하를 걸어서"가 아니라, **VU가 1,000개로 동일한 closed-loop(생각 시간 없이 반복) 구조라서 한 요청이 빨라지면 같은 시간에 더 많이 반복되기 때문**임 — 실제로 avg 응답시간 감소 배율(약 4.25배)과 RPS 증가 배율(약 4.23배)이 거의 일치해서, 이 설명이 일관됨.
- 다만 **p95(1,021.6ms)는 여전히 SLA(500ms) 대비 2배 이상 초과** — 프로젝션+캐시로 "평균적인" 응답은 크게 개선됐지만, 꼬리 구간(p95/p99/max)은 개선 폭이 상대적으로 작고 여전히 SLA를 위반함. p99(1.81s)·max(4.91s)는 9/17과 비교해도 크게 좁혀지지 않아, 일부 요청이 여전히 큰 지연을 겪는 패턴이 그대로 남아있음.
- 캐시 TTL이 2초로 매우 짧고, 1,000 VU가 동시에 몰리는 상황에서 캐시가 만료되는 순간마다 다수 요청이 한꺼번에 DB로 몰릴 수 있어("thundering herd") p95/p99 꼬리가 잘 안 줄어드는 것과 관련 있을 가능성이 있음 — 아직 검증 안 됨(가설).

### 서버·애플리케이션 / PostgreSQL / Redis
- 이번 실행에서도 Grafana/docker 지표는 **수집하지 않음** — CPU/Memory/JVM Heap/GC, HikariCP 커넥션 풀·Lock Wait, Redis 응답 지연 등은 여전히 확인 필요 (9/15~9/17 결과에서도 동일하게 미수집 상태로 남아있던 항목들).
- 특히 캐시가 실제로 히트하고 있는지(적중률)를 직접 확인하지 않음 — Caffeine은 JVM 로컬 메모리라 Prometheus에 별도 메트릭을 노출하도록 설정하지 않는 한 외부에서 관찰하기 어려움. `CacheManager`/`CaffeineCache` 통계(hit/miss)를 Actuator에 노출하는 방안을 고려할 수 있음.

### 병목 분석
- 최초 병목 지점: 이번 1,000 VU 단일 지점만으로는 "몇 VU부터 위반이 시작되는지" 재확인 불가 (300/500 VU 재측정 필요).
- 판단 근거: p95/p99/max 개선 폭이 avg/p50 개선 폭보다 뚜렷하게 작음 — 평균적인 케이스는 프로젝션(적은 컬럼 SELECT)로 빨라졌지만, 꼬리 지연을 만드는 원인(추정: DB 커넥션 대기, 캐시 미스 순간의 몰림, GC 등)은 이번 조치로 해소되지 않은 것으로 보임.
- 추정 원인: 확인 필요 — 캐시 미스 시점의 동시 DB 조회 몰림(thundering herd), HikariCP 풀 대기, GC 정지시간 등이 후보. Grafana/HikariCP 지표 없이는 특정 불가.
- 안정적으로 처리 가능한 범위: 확인 필요 (300/500 VU 재측정 및 서버 지표 확보 후 판단)
- 부하 종료 후 정상화 여부: 확인 필요 (wrapper 스크립트를 안 쓰고 단일 k6 명령으로 실행해서, 종료 후 `pg_stat_activity` 활성 커넥션 수를 자동으로 기록하지 않음)

### 최신 판정 (2026-09-17 기준)
- 테스트 결과: **FAIL** (`http_req_duration p(95)<500ms` threshold 위반 — 1,021.6ms) — 다만 9/17 대비 p95가 약 2.24배, 평균이 약 4.25배 개선된 것은 명확한 진전임.
- 진행 상태: **분석 중** (프로젝션+캐시는 "평균 레이턴시"에는 뚜렷한 효과가 있었으나, "꼬리 지연"과 SLA 통과 여부에는 아직 미흡 — 원인 규명 필요)
- 판정 근거: 오류율은 0%로 유지되지만, p95가 여전히 SLA(500ms)의 2배를 초과함
- 추가 확인 사항:
  - [ ] 300/500 VU도 동일 조합(프로젝션+캐시 on)으로 재측정해서 전체 단계별 곡선 확보
  - [ ] Grafana CPU/JVM Heap/GC, HikariCP 커넥션 풀, Redis 응답 지연 — 이번 실행도 미수집인 채로 남음
  - [ ] 캐시 히트율 실측 (현재는 캐시가 실제로 얼마나 맞고 있는지 관찰 불가)
  - [ ] 캐시 미스 시 몰림(thundering herd) 가설 검증 — TTL을 늘려서 순수 캐시 효과만 분리 측정하거나, `@Cacheable(sync = true)` 적용 후 재측정
  - [ ] 프로젝션만 켠 단독 조합, 캐시만 켠 단독 조합도 각각 측정해서 두 개선안의 개별 기여도 분리 (지금은 "둘 다 켠" 조합만 측정됨)

---

## 🔄 업데이트 (2026-09-17, 이어서): 300 / 500 VU도 프로젝션+캐시 적용 상태로 재측정

### 테스트 조건
- 코드 상태: 위 1,000 VU 재측정과 동일 — `SeatListReader.PROJECTION_ENABLED = true`, `SeatListCacheConfig.CACHE_ENABLED = true`, `CACHE_TTL_SECONDS = 2`.
- 이번엔 5단계 wrapper(`run-seat-list-load-steps.sh`)로 실행해서 300 VU → (정상화 대기) → 500 VU 순서로 결과 확보. 1,000 VU 단계는 시작 배너까지만 확인되고 이어지는 로그가 없어 이번 결과에는 포함하지 않음 — 위 09-17 1,000 VU 수치(단독 실행분)는 그대로 유지.
- 원본 로그 경로: 확인 필요 (wrapper 실행 시 `tee`로 저장한 로그 파일 경로를 알려주시면 이 항목 보완).

### 결과: 300 VU

| 지표 | 값 |
|---|---|
| avg | 45.51ms |
| p50 | 36.98ms |
| p95 | 104.81ms |
| p99 | 177.75ms |
| max | 485.67ms |
| 오류율 | 0.00% (0/526,221) |
| RPS (http_reqs) | 4,925.46/s |
| 업무 TPS (iterations) | 4,910.02/s |
| 응답 크기 | avg 7,978.99B |
| THRESHOLDS | `p(95)<500` ✓ (104.81ms) / `rate<0.01` ✓ (0.00%) → **PASS** |

### 결과: 500 VU

| 지표 | 값 |
|---|---|
| avg | 81.74ms |
| p50 | 70.65ms |
| p95 | 185.05ms |
| p99 | 294.53ms |
| max | 814.38ms |
| 오류율 | 0.00% (0/488,893) |
| RPS (http_reqs) | 4,427.74/s |
| 업무 TPS (iterations) | 4,398.30/s |
| 응답 크기 | avg 7,978.99B |
| THRESHOLDS | `p(95)<500` ✓ (185.05ms) / `rate<0.01` ✓ (0.00%) → **PASS** |

### 09-17(페이지네이션만) 대비 비교 — p95 기준

| VU | 09-17 (페이지네이션만) p95 | 09-17 (+ 프로젝션 + 캐시) p95 | 변화 |
|---|---|---|---|
| 300 | 687ms | 104.81ms | 약 6.55배 감소, **FAIL → PASS** |
| 500 | 1.57s (1,570ms) | 185.05ms | 약 8.48배 감소, **FAIL → PASS** |

### 해석
- 09-17 재검증에서 300 VU부터 SLA(p95<500ms)를 위반했던 패턴이, 프로젝션+캐시 적용 후에는 300/500 VU 모두 명확히 PASS로 전환됨 — 오류율도 두 단계 모두 0.00%.
- 반면 1,000 VU에서는 여전히 p95(1,021.6ms)가 SLA를 초과함. 즉 "프로젝션+캐시를 적용해도 꼬리 지연이 전혀 안 줄어든다"기보다는, **300~500 VU 구간까지는 이번 개선으로 충분히 해결됐고, 1,000 VU 부근에서만 한계가 나타나는 패턴**으로 재해석할 수 있음.
- 다만 이번에도 Grafana/HikariCP/JVM 등 서버 지표는 수집되지 않아, "왜 유독 1,000 VU에서만 나빠지는지"는 여전히 가설 단계(DB 커넥션 풀 대기, 캐시 미스 몰림, GC 등).

### 최신 판정 갱신 (2026-09-17, 300/500 VU 반영)
- 300 VU: **PASS**, 500 VU: **PASS**, 1,000 VU: **FAIL**(p95 1,021.6ms) → VU별로 갈리는 결과라, 전체 시나리오 판정은 1,000 VU 기준으로 여전히 **FAIL**이지만 300/500 VU 구간은 이번 개선으로 해결됨.
- 후속 작업 체크리스트 갱신:
  - [x] 300/500 VU를 프로젝션+캐시 켠 상태로 재측정 → 완료 (둘 다 PASS)
  - [ ] 1,000 VU 부근에서만 p95가 다시 악화되는 원인 규명 (Grafana/HikariCP/JVM 지표 확보 필요 — 여전히 미수집)
  - [ ] (기존 항목 유지) 캐시 히트율 실측, thundering herd 가설 검증, 프로젝션/캐시 개별 기여도 분리 측정
